**Expected Outcome**

The expected outcome of this program is to successfully establish a connection between Python and a MySQL database and perform all basic CRUD operations. The program should be able to create a table in the database, insert new records, retrieve and display existing records, update specific records based on conditions, and delete records from the table. All database changes should be saved correctly and reflected when data is fetched.

**Actual Outcome**

The Python program successfully connected to the MySQL database using mysql-connector. The student table was created without errors. Records were inserted into the table and displayed correctly using the SELECT operation. Update operations modified the specified records as expected, and delete operations removed the selected records from the database. The output matched the expected results, confirming that CRUD operations were executed successfully.