**Expected Outcome**
* Enable user login using authentication.
* Restrict application features using authentication checks.
* Create a dashboard accessible only to logged-in users.
* Implement a CRUD module (Create, Read, Update, Delete).
* Ensure proper navigation between modules.
* Maintain a clean and modular backend project structure.

**Actual Outcome**
* Successfully implemented user registration with a simplified form.
* Login functionality works correctly using Django authentication.
* Unauthorized users are redirected to the login page.
* Dashboard is accessible only after successful login.
* CRUD operations (Create, View, Update, Delete) function correctly.
* Authentication and authorization are properly enforced.
* Project is organized inside the folder Week4_Backend_Project.

